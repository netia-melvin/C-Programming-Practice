/*
 * Is the remainder result after division of two numbers
 * Only used with integers
 * Using with floats will give an error
 *
*/ 

#include <stdio.h>
int main() {
	int x = 12;
	int y = x%5;
	printf("%d", y);	
	
return 0;
}
